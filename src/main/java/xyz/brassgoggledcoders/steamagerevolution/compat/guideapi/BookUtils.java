package xyz.brassgoggledcoders.steamagerevolution.compat.guideapi;

public class BookUtils {
	// public static AbstractMap.SimpleEntry<ResourceLocation, EntryAbstract> createStandardEntry(String keyBase, String
	// keySuffix,
	// ItemStack icon) {
	// List<IPage> entry = new ArrayList<IPage>();
	// entry.addAll(PageHelper.pagesForLongText(TextUtils.localize(keyBase + keySuffix + ".info")));
	// entry.add(PageIRecipe.fromJson(new ResourceLocation(SteamAgeRevolution.MODID, keySuffix)));
	// return new AbstractMap.SimpleEntry<ResourceLocation, EntryAbstract>(
	// new ResourceLocation(SteamAgeRevolution.MODID, keySuffix + "_entry"),
	// new EntryItemStack(entry, keyBase + keySuffix, icon));
	// }
}
